#Crie um programa para calcular a área de um triângulo dado o valor da base e da sua altura.

base = float(input('Digite uma base: '))
altura = float(input("Digite uma altura: "))

area = (base * altura) / 2

print(f"{area}")